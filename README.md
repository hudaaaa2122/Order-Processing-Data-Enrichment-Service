# Order-Processing-Data-Enrichment-Service
i have build a service called Order Enricher. It receives raw order events (via REST for the sake of this test), fetches relevant customer and product information, enriches the data, stores it, and exposes the enriched orders through an API.
